# 全量待办总览 — 所有未实现的需求/问题（截至 2026-08-02）

> 汇总整个项目上下文（含历次会话）中**尚未实现**的需求与问题，作为总账。每项开工前单独确认范围并进入对应任务。

## 一、本轮新增需求（已建任务）

| 任务 | 类型 | 优先级 | 摘要 |
|---|---|---|---|
| `08-02-recent-mostread-detail` | 需求 | P2 | 最近阅读/最多阅读点击进详情页而非直接阅读（当前 L258/L288 直连 `openBook`，与书架 L399 不一致） |
| `08-02-cache-tier-review` | 分析+清理 | P2 | thumb/ 为无写入死缓存层（建议删）；download/ 为 WebDAV 整本下载回退目录（保留/清理策略待定） |
| `08-02-cover-loading-perf` | 性能 | P2 | 海报墙封面加载慢：4 并发解码、WebDAV 每封面网络往返、质量切换缓存 miss 等待测量 |
| `08-02-task-drag-reorder` | 需求 | P2 | 超分任务列表 + 拖拽排序（排队中可拖、顺序持久化） |

## 二、早期 backlog（08-01-m2-backlog 原条目）

| # | 需求/问题 | 类型 | 状态 |
|---|---|---|---|
| 3 | 标签管理元数据标签分层折叠（一级类别 → 展开具体标签） | 功能 | 已建任务 `08-02-tag-meta-hierarchy` |
| 4 | 阅读器缩放后移动区域只在第一页生效（翻页后失效） | Bug | 已建任务 `08-02-reader-zoom-pan-bug` |
| 5 | 漫画文件后缀名变更识别（zip→cbz 视为同一漫画） | 功能 | 已建任务 `08-02-extension-alias-dedup` |
| 6 | 本地漫画转 CBZ（ZIP 转 CBZ、文件夹图片打包 CBZ） | 功能 | 已建任务 `08-02-export-cbz` |
| ~~7~~ | ~~后台计划超分~~ | — | **已由 `08-01-ai-background-upscale` 实现**（含持久化续跑/悬浮窗/完成提示/阅读器版本切换） |

## 三、规划任务（已建 PRD，未开工）

| 任务 | 优先级 | 摘要 |
|---|---|---|
| `08-01-data-layer-sync` | P2 | 数据层同步就绪：SQLite 唯一真源 + 版本化标准包 + 本地优先 WebDAV 同步（P0-P4 分阶段，含书稳定 ID/墓碑/游标） |
| `08-01-exit-behavior` | P2 | 全局设置退出行为：彻底退出/后台运行/每次询问（需托盘基建） |
| `08-01-first-run-cache-guide` | P2 | 首次启动缓存目录设置引导 |
| `08-01-detail-page-stats` | P2 | 详情页显示总页数与已读页数 |
| `08-01-detail-info-settings` | P2 | 全局设置自定义详情页信息显示开关 |
| `08-01-cache-dir-migration` | P1 | **已完成并归档**（整个根目录自动迁移/数据留在所选目录/启动恢复/中断恢复），仅列作对照 |

## 四、待用户验证/观察中的项

- `08-01-ai-background-upscale`：逐页进度修复（`127e6c1`）待桌面端确认（0/N 应逐页递增、排队任务不再显示 0/0、失败红字提示）。
- 已知性能提示：Debug 构建下 Rust image 编解码较慢（约 7s/页），Release 构建约 1s/页；批量 API 已保留，Release 下可切回。

## 五、已完成（对照，避免遗漏）

- 标签持久化修复（`8a99f95`，已归档）+ 标签 hash ID 归一化（`ea0916b`，修复"数据保存失败"）
- 缓存目录切换重构（`ebdadcd`/`70a5b5a`，已归档）
- AI 超分遗留 BUG（`a50eeef`：进程超时/黑窗/并发临时文件/按书清理缓存/批量接线/原子缓存/JPEG90/缓存 key/文档/ONNX 残留）
- 后台超分队列（`e5cdb40`）+ 进度修复（`85d7005`/`3345942`/`7f9c85a`/`127e6c1`）
- 阅读器原版/超分版本切换、完成提示、重启续跑、取消语义

## Notes

- 每项开工前：确认验收目标、优先级、依赖；`#4` 依赖阅读器改造现状；`08-02-task-drag-reorder` 依赖 `ai_tasks` 表结构（加 sort_order）。
- 2026-08-02 更新：阅读器核心 backlog（双页拼接自动模式 / 滚轮缩放 / 自定义按键绑定）已按用户要求删除，不做；另新增 `08-02-avif-support`、`08-02-reader-page-rotate` 两个规划任务。
