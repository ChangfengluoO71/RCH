# CLAUDE.md

## 全局原则

* 始终使用中文回复。
* 优先保持项目稳定，不随意修改已有设计。
* 优先阅读文档，再开始编码。
* 未经确认，不进行架构级修改。
* 所有重要操作必须可追溯。
* **Bug 排查遵循"先分析后动手、双向反馈"原则**：
  - 用户报告 bug → 先和用户对齐现象、提出可能根因假设 → 用户确认方向 → 再动手修改。
  - 不一条线走到底：修改后让用户验证当前修改是否正确，有了回音再继续下一步。
  - 不反复"改→编译→启动→失败→再改"循环；先读代码静态分析定位根因，一次改对后再启动验证。
  - 遇到问题先告知用户你的判断和可能原因，而不是沉默地反复尝试。记住：你是和人协作，不是单方面执行任务。

---

# 2025-07-28 标签补全失效 Bug 反思

## 问题现象
1. 用户输入 `#彩` → 补全列表弹出"彩漫"标签 → 点击标签后搜索框无反应，不自动填补。
2. 后续新增的标签在搜索框输入 `#` 后根本不出现补全列表。

## 根因分析（系统性问题）

### 表面根因
`_onSearch` 在 `onChanged` 中被调用，补全 `onTap` 通过 `_searchCtrl.text = ...` 赋值触发 `onChanged` 二次调用，形成时序冲突。

### 深层系统性问题
1. **`allTags()` 只从 `metas` 收集标签**，而 `metas` 只包含已打开过的漫画的元数据。纯新增标签（从未关联到任何漫画）不会出现在补全列表中。
2. **标签数据模型设计缺陷**：标签存储在 `BookMeta.tags`（per-book），没有独立的全局标签表。`allTags()` 需要遍历所有 `metas` 才能收集标签，且标签删改后如无漫画关联就彻底消失。
3. **`library.json` 持久化静默失败**：`_save()` 的 `catch (_) {}` 吞掉了所有异常，如果写入失败（权限、磁盘满等），标签数据丢失且无任何提示。
4. **补全列表点击可能未被触发**：Overlay 中的 `ListTile` 可能被 Material 祖先的 ink splash 问题拦截，或者 `_hideOverlay()` 在 `onTap` 回调执行前就被某个重建周期移除。

## 排查过程的问题

1. **没有先分析数据流再动手**：多次修改 `onTap` 的字符串拼接逻辑，但从不检查 `_onSearch` 里 `onChanged` 触发的完整流程。

2. **反复启动应用而不验证代码**：每次改完就 `flutter run -d windows` 等待 20 秒看结果，结果总是不对。正确做法是先静态分析 `_onSearch` → `onChanged` → `_onSearch` (二次触发) → `_showOverlay` 的完整链路，在脑中过一遍流程，发现逻辑漏洞。

3. **没有充分理解问题了再动手**：应该先用 print/日志确认是 `textController` 没写入，还是补全后 `_onSearch` 没有正确执行，还是 `_selectedTags` 值不对。

4. **未遵从"双向反馈"原则**：用户多次反馈"还是没效果"，每次都在改代码，而不是停下来和用户确认具体现象。

## 改正方向

* bug排查的第一步永远是**静态分析**，不依赖运行时的反复试错。
* 修改前先分析数据流：谁改了 Controller、谁触发了 `onChanged`、谁更新了 state、state 变化后 UI 如何重建。
* 修改后让用户验证，收到反馈再继续，不积累未经验证的改动。
* 遇到用户说"还是没效果"时，立即停下来，和用户对齐具体现象（是标签不出现，还是点击不填补，还是筛选不出结果），而不是再改一轮代码。

---

# 文档规范

项目维护以下文档：

## README.md

描述项目**当前状态**。

包含：

* 项目简介
* 当前功能
* 如何运行
* 目录结构
* 配置说明
* 已实现能力
* 已知问题（可选）

README始终反映**当前可运行版本**。

---

## SPEC.md

描述项目**目标状态**。

包含：

* 项目目标
* 设计原则
* 技术选型
* 系统架构
* 功能规划
* 开发规范
* 验收标准
* 不做什么（Out of Scope）

SPEC是项目最高设计文档。

**原则上不得修改。**

只有以下情况允许修改：

* 当前方案不可行
* 技术栈需要整体调整
* 用户明确确认修改SPEC

否则，一律按照SPEC实施。

---

## LOG.md

记录项目开发历史。

每次施工必须追加：

* 日期
* 本轮目标
* 修改内容
* 修改原因
* 影响范围
* 是否完成
* 遗留问题

不得覆盖历史记录。

只能追加。

---

## LOG-INDEX.md

LOG索引。

记录：

* 日期
* 本次标题
* LOG起始行
* LOG结束行
* HASH

用于快速定位历史记录。

---

## DECISION.md（架构决策记录，ADR）

只记录重大设计决策，不记录施工。

例如：

为什么从 SQLite 改 PostgreSQL
为什么采用 CQRS
为什么弃用 Redis
为什么 REST 改 gRPC

这类内容不适合放 LOG，因为它们是长期有效的设计依据。

---

## TODO.md

记录未来计划，而不是聊天里的临时需求。

建议采用：

Backlog
Doing
Waiting
Done

这样 Agent 每次进入项目时，就能快速了解当前工作状态。

---

# 开发流程

任何开发任务均遵循：

1.

阅读：

* README
* SPEC

确认当前项目状态。

2.

分析用户需求。

判断是否属于：

* Bug修复
* 功能开发
* 重构
* 架构调整

3.

若属于：

* 架构修改
* 数据结构调整
* API重大变更
* 技术栈调整
* 删除已有能力

必须先征求用户确认。

不得直接施工。

4.

开始开发。

保持：

* 小步提交
* 最小修改
* 不影响已有功能

5.

施工结束：

先更新LOG。

等待用户验证。

6.

用户确认无误后：

更新README。

如果涉及SPEC变更：

必须再次获得确认。

---

# 编码原则

始终遵循：

* KISS
* SOLID
* DRY
* YAGNI

避免：

* 重复实现
* 过度设计
* 魔法数字
* 隐式依赖

优先：

* 可维护性
* 可读性
* 可测试性

---

# 修改原则

尽量：

* 修改已有代码

不要：

* 重写整个文件
* 推倒重来
* 无关重构

除非用户明确要求。

---

# 文件原则

新增文件前：

优先确认：

是否已有相同职责文件。

避免：

* test2.py
* new_main.py
* final_v3.py

保持目录整洁。

---

# 日志规范

LOG必须说明：

为什么改。

不仅记录：

改了什么。

重点记录：

设计决策。

例如：

错误：

> 修改login接口。

正确：

> login接口增加Refresh Token支持，原因是JWT有效期缩短后用户体验下降，同时避免频繁登录。

---

# 用户确认规则

以下操作必须确认：

* 修改SPEC
* 删除功能
* 删除文件
* 修改数据库结构
* 修改API
* 修改目录结构
* 更换技术栈
* 大规模重构

否则直接开发。

---

# 禁止事项

未经确认：

* 不修改SPEC
* 不删除代码
* 不删除文档
* 不改变接口
* 不改变数据库
* 不更换框架

不得：

* 编造运行结果
* 编造测试结果
* 编造完成情况

如果无法验证：

明确说明。

---

# 文档更新规则

| 文件           | 更新时间                                        |
| ------------ | ------------------------------------------- |
| TODO.md      | 用户提出新需求后立即更新；任务开始时调整状态；任务完成后更新状态；用户确认后归档或移除 |
| LOG.md       | 每次施工完成立即更新，不得遗漏                             |
| LOG-INDEX.md | LOG 新增记录后立即同步更新                             |
| README.md    | 用户确认本轮功能正确后更新，保持与当前版本一致                     |
| SPEC.md      | 仅在用户确认修改设计目标或技术路线后更新                        |
| DECISION.md  | 发生重大设计决策且已确定方案后立即更新，不记录普通开发过程               |
