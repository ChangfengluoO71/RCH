# LOG 索引(LOG-INDEX)

> 登记 `LOG.md` 的:日期 + 标题 + LOG起始行 + LOG结束行 + HASH,用于快速定位历史记录。
> HASH 为该轮登记时 `LOG.md` 全文的 SHA256(快照),用于校验日志是否被改动。

| 日期 | 标题 | 起始行 | 结束行 | HASH(SHA256) |
|---|---|---|---|---|
| 2026-07-26 | 第 1 轮:立项与总体规划 | 9 | 32 | 65e7e8be… |
| 2026-07-26 | 第 2 轮:环境就绪、工程骨架与最小阅读链路 | 33 | 55 | df703e0c… |
| 2026-07-26 | 第 3 轮:翻页流畅(缓存+预取)与海报墙书架 | 56 | 71 | df703e0c… |
| 2026-07-26 | 第 4 轮:修复阅读器缩放失效 + 建立文档体系 | 72 | 100 | f8587ea2… |
| 2026-07-26 | 第 5 轮:滚轮自由缩放 + 阅读器改单页 PhotoView | 101 | 128 | 3bdaa45f… |
| 2026-07-27 | 第 6 轮:接入 WebDAV 书源(远程流式阅读) | 129 | 158 | 2dc20345… |
| 2026-07-27 | 第 7 轮:修复 WebDAV 连接失败时的 tokio runtime panic | 159 | 181 | d0857a05… |
| 2026-07-27 | 第 8 轮:主界面(书源/最近/最多/搜索)+ WebDAV 流式性能重构 | 182 | 209 | 9baed1c0… |
| 2026-07-27 | 第 9 轮:修复书源切换不刷新 + L2 磁盘缓存(WebDAV 提速) | 210 | 236 | 0d00a944… |
| 2026-07-27 | 第 10 轮:设置模块 + 漫画详细界面 + 封面自定义 + WebDAV 海报墙 | 350 | 380 | (snapshot) |
| 2026-07-27 | 第 10 轮:WebDAV 请求精简 + 错误信息改进 + 整包下载优化 + 文档体系补全 | 237 | 278 | d9517baa… |
| 2026-07-27 | 第 11 轮:缓存管理面板 + 失效数据清理 + 未来拓展规划 | 279 | 311 | ce376891… |
| 2026-07-27 | 第 12 轮:双页拼接精简 + 文档总结(遗留问题记录) | 312 | 349 | 7867afe0… |
| 2026-07-29 | 第 13 轮:条漫缩放冲突修复 + 自定义按键绑定(未完成,保留现场) | 381 | 417 | (snapshot) |
| 2026-07-30 | 第 14 轮:元数据标签系统 + 批量打标签 + 书源详情页 + 标签管理面板 | 418 | 451 | (snapshot) |
| 2026-07-30 | 第 15 轮:书源编辑完善 + 文件夹复选框批量选择 + home_page.dart 格式化 | 453 | 483 | (snapshot) |
| 2026-07-30 | 第 16 轮:M2/M3/M5 技术方案决策与 SPEC 修订 | 484 | 550 | (snapshot) |
| 2026-07-30 | 第 17 轮:archive/ → document/ 重构（Document trait + DocumentMeta） | 551 | 583 | (snapshot) |
| 2026-07-30 | 第 18 轮:EPUB 格式支持（ZIP + OPF spine 自研，零新增依赖） | 585 | 622 | (snapshot) |
| 2026-07-30 | 第 19 轮:Folder+CB7+CBT+PDF+CBR+MOBI 6种格式一次性实现 | 623 | 660 | (snapshot) |
| 2026-07-30 | 第 20 轮:MOBI 直接解析 + WebDAV 封面懒加载 | 661 | 689 | (snapshot) |
| 2026-07-27 | 第 22 轮:修复 WebDAV 封面不加载 + 添加下载进度指示 | 691 | 716 | (snapshot) |
| 2026-07-28 | 第 23 轮:缓存管理面板分级 + 下载百分比进度 + 封面缓存策略优化 | 718 | 745 | (snapshot) |
| 2026-07-28 | 第 24 轮:修复标签补全点击无反应 + 开发流程总结 | 747 | 768 | (snapshot) |
| 2026-07-28 | 第 25 轮:标签补全失效系统性问题分析 + 文档更新 | 775 | 805 | (snapshot) |
| 2026-07-28 | 第 26 轮:实施 ADR-016/017 — Repository + Tag 独立建模 + 全量架构评价 | 807 | 836 | (snapshot) |
| 2026-07-28 | 第 27 轮:标签系统重做 — 全面兼容新架构 + 搜索系统重新设计 | 831 | 874 | (snapshot) |
| 2026-07-28 | 第 28 轮:跨书源搜索重新设计 — 搜索栏内联补全 + globalSearch API | 876 | 907 | (snapshot) |
| 2026-07-28 | 第 29 轮:搜索系统统一 — 筛选/全局共用搜索逻辑 | 908 | 935 | (snapshot) |
| 2026-07-28 | 第 30 轮:Git 初始化 + 数据层稳定化 + Folder 元数据全面支持 | 937 | 997 | \37cc871853b |
| 2026-07-28 | 第 31 轮:开源治理—文档体系建设 | 999 | 1032 | 6e9316f |
| 2026-07-28 | 第 32 轮:SQLite 数据层迁移（Phase 1 + 2） | 1034 | 1121 | 8f3a2b1 |
| 2026-07-28 | 第 33 轮:标签 ID 重构 + 标签管理 Bug 修复 + 已读标签 | 1123 | 1147 | f1e5c7d |
| 2026-07-28 | 第 34 轮:已读元数据标签完善 + 应用安装程序 + 使用文档 | 1149 | 1198 | a3d8f1c |
| 2026-07-28 | 第 36 轮:Repository 层扩展到 Book + Record | 233 | 280 | 8f093f72… |
| 2026-07-28 | 第 38 轮：封面加载限流（并发队列 + dispose 取消） | 333 | 383 | 99046c02… |
| 2026-07-31 | 第 39 轮：M2 AI 超分 Phase 1 — CLI 单次调用方案 | 384 | 464 | (pending) |
| 2026-07-31 | 第 39.1 轮：详情页整本 AI 超分 +「AI超分」元数据标签 | 465 | 494 | (pending) |
| 2026-07-31 | 第 40 轮：M2 AI 超分 Phase 2 — CLI 目录批量模式 + ONNX 评估 | 495 | 545 | (pending) |
| 2026-08-08 | 第 41 轮：阅读器触屏修复 + 应用内更新系统（Windows + Android） | 569 | 608 | E7BB065F |
| 2026-08-08 | 第 41.1 轮：P4 合入 — Android 正式签名 | 609 | 639 | A9F2DF45 |
| 2026-08-08 | 第 42 轮：书源同步导出补全（手动导出到文件 / 加密书源凭据包 / Android 降级） | 640 | 662 | 07231896 |
| 2026-08-17 | 第 43 轮：RCH项目组使用反馈修复（条漫页码 / 书源顶栏重叠 / PC 图标） | 664 | 712 | 6F467865 |
| 2026-08-21 | 第 44 轮：修复「清理失效漫画数据」无效（远程删除漫画后缓存与数据库残留） | 714 | 931 | 9474F2D8 |
| 2026-08-28 | 历史对账（release record）：v0.5.6 / v0.5.7（非实时日志） | 986 | 1000 | — |
| 2026-09-18 | 第 47 轮：P1 全量闭环（A–F：cover 状态机 / wake / 补偿 / disk-first / 事件驱动 / 进度语义） | 1002 | 1038 | fcb83bc |
| 2026-09-18 | 第 48 轮：RG-A（Release Gate A 类自动化）—— Range fallback / atomic raw-cache / HTTP 语义 | 1039 | 1085 | 03a4be7 |
| 2026-09-18 | 第 49 轮：RG-B B-0/B-0.1/B-1 —— RC 身份冻结与真实 FRB 跨桥 delivery | 1086 | 1119 | 12c8fb6 |
| 2026-09-19 | 第 50 轮：性能缺陷修复（封面进度 fs 扫描）—— 启动卡顿 / 刮削慢 | 1120 | 1154 | 9cbc840 |
| 2026-09-19 | 第 51 轮：封面失败安全子原因（① `730ce77`）+ 长图/长 PDF 分级窗口 / 顶部裁带 / 具体失败码 / `bytes_fetched`（③-1） | 1156 | 1224 | (pending) |
| 2026-09-19 | 第 52 轮：②-b 有界回填取数 —— 275 个夸克封面失败根因＝缺 `pdfium.dll`（A/B 坐实）+ ③-1 复测修正 3 处（perf 保留键 / 归档字节计数 / 原生库缺失自证码） | 1226 | 1293 | (pending) |
| 2026-09-19 | 第 53 轮：② 自愈策略（可修复失败进 6h 长期补偿）+ 测试根隔离（`cache_root` 测试构建锚定临时根）+ CI 串行 + pdfium 复测（267 PDF＝部署问题 / 8 MOBI 行＝真问题） | 1295 | 1363 | (pending) |
| 2026-09-19 | 第 54 轮：③ 格式专用 —— MOBI 封面定位修复（`image_records` 黑名单漏进 CSS/HTML）+ 魔数嗅探诊断 + 有界换页兜底 + triage 工具 session_epoch 对齐 | 1365 | 1432 | (pending) |
| 2026-09-19 | 第 55 轮：真实库扫描审查 —— 115 源 225 个 `notFound`＝**同指纹双源 `library_index.id` 冲突**（结构性根因）+ 按指纹解析 + 自愈重挂 (1b) + `scan_diag` 诊断通道 | 1434 | 1513 | (pending) |
| 2026-09-19 | 第 56 轮：书源页刷新按钮 + 删除流兜底 + “删了没反应”诊断（删除成功、但 `remote_scan_*` / `remote_cover_*` 共 10 张表残留） | 1515 | 1561 | (pending) |
| 2026-09-19 | 第 57 轮：删除书源彻底化（补删 `remote_*` 17 张表）+ 启动孤儿清理（真实库预测清 53 行；无需改 Dart/FRB） | 1563 | 1609 | (pending) |
| 2026-09-19 | 第 58 轮：重建 + 重启 + 端到端验证（启动清理 53 行命中预测、⟳ 按钮视觉确认、测试源已消失） | 1613 | 1638 | (pending) |
| 2026-09-19 | 第 59 轮：同名书源消歧 + 刷新/删除失败可见（SnackBar+scan_diag）+ `setState` 守卫 + **恢复「选择文件夹自动填 cid」入口**（含历史考证：该选择器从未接线） | 1642 | 1689 | (pending) |
| 2026-09-19 | 第 60 轮：封面“很慢”量化根因（2.08GB 病态 zip 的 EOCD 尾部回扫 233MB）+ 单封面读取预算（字节/次数/挂钟三重上限）+ 具体码 `cover_read_budget_exceeded` | 1693 | 1750 | (pending) |
| 2026-09-19 | 第 61 轮：启动期回收**过期封面租约**（重启后 job 永不重试、界面永远“进行中”）+ 用例；门禁 506/0 | 1754 | 1778 | (pending) |
| 2026-09-19 | 第 62 轮：调研 —— 封面打开成本；**C 不可实现**（crate 无 API），推荐 D 封面专用最小 ZIP 读取器（4–6 次请求 O(1)）；更正第 60 轮“尾部回扫”误判为“每条目校验读” | 1782 | 1823 | (pending) |
| 2026-09-19 | 第 63 轮：落地 D（封面专用最小 ZIP 读取器）；A/B：2GB CBZ 由 192 次读被截断 → **8 次读 9 秒拿到封面**；预算码改可重试 + (1b) 桶纳入 | 1827 | 1879 | (pending) |
| 2026-09-19 | 第 64 轮：启动时自动重连已保存凭据的书源（`warmUpSessions`，零点击恢复会话/扫描/封面）+ 快通道后实测吞吐 ≈14 枚/分钟（约 6×） | 1883 | 1922 | (pending) |
| 2026-09-19 | 第 65 轮：遗留码重挂的缺口＝**触发器**（补“扫描完成即 reconcile”，夸克 promoted=64 → failed 284→92、ready +13）+ 重扫失败提示修正 | 1926 | 1974 | (pending) |
| 2026-09-19 | 第 66 轮：夸克书源**扫码登录**（Rust 3 函数 + FRB codegen + Dart 扫码对话框 + 两个对话框按钮）；真机验证 token 接口 ✓；门禁 511/0 | 1978 | 2024 | (pending) |
| 2026-09-19 | 第 68 轮：P1 自适应预读（256KiB→1MiB，连续 3 次后提升）+ 回落保险 + 3 条契约用例；**真实会话 A/B 尚未成立**（如实记录） | 2028 | 2070 | (pending) |
| 2026-09-19 | 第 69 轮：默认流式 + `auto` 语义翻转（115/夸克/百度），治“ZIP 打开整本下载”（实测 49MB 书打开传 12.26MB→改后按需读）；遗留自动删包 | 2074 | 2106 | (pending) |
| 2026-09-19 | 第 70 轮：**P0 落地**——打开 ZIP 只读中央目录（800 条目 ⇒ 打开 ≤4 次读，改前 800+）；契约改到网络层（用户选 A） | 2110 | 2148 | (pending) |
| 2026-09-19 | 第 71 轮：自动删除整包（只删 raw、封面保留；设置项默认关 + 界面开关 + 关书钩子） | 2152 | 2182 | (pending) |
| 2026-09-19 | 第 72 轮：设置项按类别折叠（13 组 → 5 类：阅读/书源与网络/缓存与存储/同步与备份/外观与布局） | 2186 | 2212 | (pending) |
| 2026-09-19/20 | 第 73–77 轮：二维码保存修复（RGBA→PNG）+ 115 微信提示 · 文案说反修正 · 对比度 80→3 处 · 全格式审查（EPUB/PDF/MOBI/7Z/TAR/RAR 六种走老路，RAR 最严重）+ EPUB `CdArchive` 适配层（未接线）· 安卓真机打包踩坑记录 | 2216 | 2270 | (pending) |
| 2026-09-20 | 第 77 轮附录：`epub.rs` 精读结论（200/474 行）——现有结构与最小侵入改造点（访问层模仿 `ZipFile` 接口 + `data_start` 惰性 + 按需解析） | 2272 | 2299 | (pending) |
| 2026-09-20 | 第 78 轮：EPUB 打开优化（三步一次成型）——`CdArchive` 接线（只读 EOCD+中央目录）+ 起点惰性缓存 + 章节按需解析 + 探针本地 A/B；**300 条目 EPUB 打开 4 次读**（改前 45/47）、全量门禁 0 失败；独立评审 2 Important 已修（大小写匹配 / 后缀误选），复审 **PASS**；**真机复核**发现快路径因"EOCD 后有 5 B 尾巴"弃权 ⇒ 放宽 EOCD 判定（严格优先 + 签名验证），真机两本 **219/204 次读 → 4 次读**（30.8/28.2 s → 0.59/0.58 s）；新增诊断工具 `quark_epub_probe` | 2303 | 2474 | 9cb725de… |
| 2026-09-20 | 第 79 轮：PDF 惰性按需读 —— 按真实库分布重排格式优先级（PDF 609 / RAR 0）；`PdfBook::open` 改 pdfium `FPDF_FILEACCESS` 按需读（`PdfFetchReader`：`SourceReader` 摊薄 + "填满或报错"以对短读契约），失败回退整份读入；真机 `1.pdf`（57.5 MB）打开+首页 **57.5 MB → 0.29 MB（197×）** 且渲染**逐字节一致**；探针更名 `quark_document_probe` 并加 `--pdf`。**续**：真机 `pdf_diag.log` 证明惰性读 8/8 生效，但长条页 1600px 单页 0.7–6.8 s/输出 7.3 MB、495 个后台封面抢 4 req/s 门控 ⇒ 加"后台封面让路（20 s 窗口）"+ `Document::page_bytes_for_display`（封面按 340px 渲染：1029 ms/3.36 MB → 36 ms/265 KB）；**续2**：真机 bug"详情页有图、海报墙获取失败"根因＝状态读取写死 `default`/`340x480@1` 而卡片按 `coverQuality=low` 取 `170x240@1`（真机 7 本 170 ready/340 failed）⇒ `remote_cover_state` 增 `selection`+`profile` 参数（含 FRB codegen）+ 契约 STATE-READ-4；**续3**：真机"一直转圈"根因＝封面预算 24MB/192 次/45 s 在 4 请求/秒门控下等于"转 45 秒以上"，且 PDF 封面会扫 4 页烧穿预算 ⇒ 挂钟 45→15 s + **PDF 扫页 4→2 页（格式感知，其它格式契约不变；门禁先挡回，未放宽既有测试）**；**续4**：真机"海报墙封面没读取"根因＝卡片 wake 分支**无条件抛异常**（state 已 ready 也不读缓存图）⇒ 改成 ready 即直读缓存、ready 但缓存缺失则回退重新获取（"有缓存就直读 / 没缓存就获取"）；**续5**：按用户确认去掉"容器文件夹卡片无 assetId 时早退"的占位行为，改为回退 legacy 取图（并发由封面加载队列限流），并让 `remoteAssetId` 变化触发重载以切回统一路径；**续6**：真机 DB 坐实"扫描按 340 抓、卡片按设置要 170"⇒ 586 本 PDF 里 340 档 ready 142 本而 170 档只有 42 本 ⇒ 卡片新增**跨 profile 回退读缓存**（本档优先，其余标准档依次尝试，命中即用）；**续7**：扫描/预览建封面任务改为**跟随设置 `coverQuality`**（新增 `cover_quality_profile_on` + 单测）；**续8**：独立评审后修复 —— Dart 测试消费者签名（C-1，且补跑 `flutter test` 8 项全过）、让路不再消耗 `attempt`（C-2，我引入的回归）、PDF 取页错误不再被吞成永久失败（C-3）、`COVER_PDF_MAX_BYTES` 128MB→512MB（I-1，正好挡住库里最大的三本）、不变量措辞收窄（C-5）；并如实记录 4 条未做（C-4 锁内网络 I/O、C-6 断言深度、I-2 键不一致、I-3 读放大） | 2478 | 2789 | e39efa44… |
| 2026-09-20 | 第 80 轮：封面重抓能力 —— 桌面「金牌得主」目录取证（EPUB 20/20 ready；MOBI 8 本、PDF `1–7.pdf` 撞 `cover_read_budget_exceeded`、`0/8/9.pdf` 撞 128MB 硬拒）且**全是终态、永不自愈**（`attempt` 3–5，正是第 79 轮 C-2 回归烧掉的）；新增 FRB `remote_cover_reset_to_current_profile`（删非当前档变体/任务/孤儿 blob+文件、当前档 `failed` 重排队且 `attempt` 归零、`blocked/unsupported` 不动、bump revision）+ 单测（含一次自伤死锁的修正记录）+ 设置页换档接线；门禁 exit 0；**补记**：用户截图 + DB 对照锁定 MOBI 瓶颈 —— `MobiBook` 整份读入 + `COVER_FETCH_LIMIT_BYTES=128MB` 硬拒 ⇒ ≤79MB 有封面（代价是整本下载）、143/146/180MB 被拒，分界与阈值完全吻合；结论：与 PDF 同类，MOBI 惰性按需读可把每枚封面 74–180MB 降到 ~1MB | 2793 | 2845 | e77e0094… |
| 2026-09-20 | 第 81 轮：MOBI 惰性按需读 + 全格式体检 —— 四个格式（mobi/sevenz/tar/rar）开头同形（`vec![0u8; len]` 整份读入）但难度差异大 ⇒ 不一起改，用户决定 TAR/7Z/RAR 保持整本下载；MOBI 改为只读「PalmDB 头 78B + 记录表 + record0 前 128B + 每条候选记录 16B 魔数」，页表存 `(offset,len)` 区间（顺带消掉 2× 内存），字段偏移取自 mobi crate 自身解析序列（不靠记忆），并保留整份解析为回退（先验后回退）；`document::mobi` 4 passed（核心断言：12MB 文件打开只读 <4096 字节），全量门禁 exit 0（402 lib） | 2874 | 2993 | 42e3bae1… |
| 2026-09-21 | 第 82 轮（重做）：封面文案统一（删「未缓存」→ 统一「等待扫描」，用户决定）+ 扫描 reconcile 档位改为用户设置（此前写死 340，真机实测每会话事件造 64 条错档任务 + bump revision ⇒ 封面文案抖动）+ revision 到达不再无条件清空 `_coverState`（只对 `running` 保留清空——第一版被 3 个终端契约测试抓到会把 running→ready 钉死在转圈）+ 桌面切 Release（Debug=JIT+opt0 vs Release=AOT+优化，手机同批远端文件更快的主因）+ 全量只读审计（D1 MOBI 未接 SourceReader 每条记录 16B 探测/G1 每页 7.8–16.1 次串行 Range·小读占 62% 耗时·offset=0 重取 108–135 次/D4 磁盘 1061 张封面因 variant=0 读不出/D3 读路径回写/D7 单条大记录不可分片/D8 误导性"下载整本"文案）；门禁 Rust 532 passed / 0 failed、Dart 34 项全过 | 2998 | 3047 | 279e3b3c… |
| 2026-09-21 | 第 83 轮：Release 产物缺 `pdfium.dll` 导致 PDF 全部打不开（Debug 那份是历史手工拷贝、未进 git；CI 只管 Release）⇒ 新增 CMake `install(FILES pdfium.dll)` 规则让两个 profile 自动带上 + 立即补齐产物；D1a **MOBI 封面专用打开**（原"合并探测窗口"经实测无效：记录头隔着整张图 ⇒ 读次数不变；改为 `MobiBook::open_cover` 只探测到第一张图就停，1–3 次读 vs 此前 200–300 次，阅读路径不变）；D4 **已下载封面只读回退**（换档 purge 只删 job/variant ⇒ `variant=0` 而 `blob/ref=1061`、1.2 GB；Rust 用 ref.owner_key 反推 content_revision 读同一份字节 + Dart 卡片在非 ready 时先做纯本地读再回退占位）；门禁 Rust 24 套件 534 passed / 0 failed（+2 新回归）、Dart 34 项全过 | 3052 | 3097 | aec6e45f… |
| 2026-09-21 | 第 84 轮：详情页「原文件名」显示 provider id 修复（末段是不透明 id 时回退目录项名称 + 回归测试）；"PDF/MOBI 还是慢"实测归因（PDF＝**每页 5 次串行 Range×~182 ms≈909 ms，与字节数脱钩 ⇒ 就是 G1**；MOBI 阅读仍逐条探测 ⇒ 非 G1 可解，需页面表缓存/渐进打开/不探测三选一）；安卓 release 包重建（116.3 MB/三 ABI，含本轮全部修复） | 3099 | 3138 | fcf45f2c… |
| 2026-09-21 | 第 85 轮：手机端 adb 安装（Success/16:52，签名兼容）+ 桌面 Release 重建重启；**MOBI 阅读加速=小探测并发取**（瓶颈是 RTT 不是带宽：136–182 ms/次仅取 ~29 B，200–300 条候选串行 30–40 s ⇒ `concurrent_probe` 默认 4 worker、按原索引写回保页序、失败仍整体弃权；封面入口保持串行 ≤6 次读；与第81轮"拆大读"的 A/B 结论不矛盾）；回归测试并发度 ≥2/封面并发度 =1；门禁 24 套件 535 passed/0 failed；查明手机剩余封面失败主要在 pdf(75)/zip(17) 且与"墙不刷新"分别指向 G1 与 ready 发布未唤醒 | 3140 | 3175 | 5c6ac8f1… |
| 2026-09-21 | 第 86 轮：**"详情页出图后海报墙不刷新"真根因**＝无 asset id 的卡片 `_attachCoverRevision()` 直接早退、从不订阅 cover revision（先证伪"ready 未 bump/notify"与"代际错位"两假设：`mark_job_ready_owned_on` :546/:564 有 bump+notify；`bump_view_revision_on` 每源一行自增）；修法=只要源需要 session 就挂监听、唤醒后重跑取图（本地优先）；analyze 干净 + 11 个文件 37 项测试全过；**该修复暂无自动化回归**（夹具到不了无 asset id 的取图出口，已删假测试并挂 TODO）；桌面 Release 重建重启（PID 38164）+ 安卓包重建并装到手机（17:34:04） | 3175 | 3213 | ce716b7 |
| 2026-09-21 | 第 87 轮：给"MOBI 打开走了哪条路"补埋点＝`reader_diag.log`（`mode=raw-cache/stream/fallback-download` + 耗时）与 `mobi_diag.log`（`mode=lazy/cover-lazy/full-fallback`、`mobi_page bytes/ms`）；此前策略只有 `tracing::*!` 而**全仓无 tracing subscriber ⇒ 不落盘**（"有没有转整本"查不到）；旁证：`cache/raw` 空但阅读器退出会删 raw 包（空≠没下载过）、`cache/page` 17:00 后新增 121 文件（逐页缓存解释"读完很流畅"）、`pdf_diag` 当前 1600 宽 ≈0.8 s/页、6 次读/页；量化结论：**"超10秒转整本"对 76 MB MOBI ≈17 s（实测 4.4 MB/s）反而更慢**，建议改做页面表缓存；交付＝双端重建（手机 17:44:50 已装）；门禁首跑挂 P0 下限 flake（要求 ≥500 ms 而实测 [8,397,102]）已如实记录 | 3219 | 3261 | (见提交) |
| 2026-09-21 | 第 88 轮：**115 原文件名 id 修复**（真机 DB 取证＝115 路径是 19 位纯数字 id；判定放宽为 hex≥24∪数字≥16∪无点长 token，并从末段向前取第一个非 id 段；3 个新用例）；**失败封面"轻量重试"逃生口**（根因：终态 failed 粘性，DB 里成片的 `cover_native_lib_missing` 来自缺 pdfium 窗口期 ⇒ 新增 `remote_cover_retry_failed` + store `requeue_failed_for_source_on`（只动当前档 failed、上限 500、重置 attempt/退避/长期补偿、bump+notify+wake）+ FRB codegen + 仓库 `retryFailed`（1..500 夹取）+ 挂到源浏览器刷新动作 + Rust/Dart 契约测试）；门禁 24 套件 536 passed/0 failed、Dart 19 项；桌面重建重启（PID 39432）、APK 重建（手机已拔未装） | 3261 | 3305 | (见提交) |
| 2026-09-21 | 第 89 轮：**MOBI 页表缓存（首开十几秒 → 之后秒开）**——键=`stable_hash(path|file_len)` → `cache/mobi_table/`（`CacheDir` 新增 `MobiTable`），自校验=`sha256(头78B‖记录表)`（版本/长度/digest 全等才命中，文件改写即使等长也失效）+ 区间防御校验 + 先 .tmp 再 rename；封面入口不写缓存；`mobi_diag` 增 `mode=lazy-cached`/`cache=hit|miss`。**单测实测 45→5 次远端读（≈9 倍；300 页书 ~41s→~0.7s）**，两条回归；mobi 测试全部隔离临时缓存根；门禁 24 套件 538 passed/0 failed；桌面重建重启（PID 15952）、APK 重建（手机未插未装） | 3265 | 3301 | 3341 |
| 2026-09-21 | 第 90 轮：**G1 = 钉住文件头窗口（自然取到就留下，零额外请求与零额外字节）+ 元数据槽 2→4**——真机签名：同一 offset=0 被重取 108–135 次、EPUB 906/1265 次小读吃掉 62 个百分点的读时长；新增契约 `tests/source_reader_head_pin_contract.rs`（并把实现 stash 掉验证它确实会挂：before=5 after=6）；A/B：P0 请求数 8（开）vs 8–9（关，更抖），P0-B2 完全一致（无读放大回归）；门禁 25 套件 540 passed / 0 failed。**D7 实测纠正方向**：屏宽 2560 > 渲染宽 1600，按屏宽渲染反而更多字节；真正成本是阅读页 out_bytes≈1.9 MB/页（FRB→Dart），降宽度属画质权衡待用户拍板，长条页切片另计 | 3350 | 3390 | (见提交) |
| 2026-09-21 | 第 91 轮：**D7 阅读渲染宽度设置**（省流 1080 / 标准 1600 / 跟随屏幕）——`RenderWidth` + `AppSettings.renderWidth`（默认 standard）+ 纯函数 `renderWidthPixels`（标准档返回 null ⇒ 页缓存路径与历史完全一致）；Rust `book_page(..., target_width)` + `Reader.display_width` 会话状态（预取沿用同宽、换宽清 L1）+ 页缓存按宽度分目录 `page/<ns>/w<宽>/`；只有 PDF 覆写 `page_bytes_for_display`。验证：Rust reader 8 项（新增宽度转发/分目录/清 L1 断言）、Dart 4 项新用例 + 相关 23 项、analyze 干净 | 3394 | 3428 | (见提交) |
| 2026-09-21 | 第 92 轮：**删掉扫描状态栏三个按钮**（暂停/继续、增量重扫、全量重扫；连带移除已死的 onPause/onResume/onRescanIncremental/onRescanFull 参数；保留"重试远程扫描"）+ **修"反复刷新也没用"**（真因：重试唤醒排在 `_relist()` 之前 ⇒ 无活会话时 `wake_cover_worker_for_source` 直接返回、任务只排回 pending；已把重试移到 relist 之后）+ 回答刷新语义（relist＋失败封面重排，两件事）。门禁 25 套件 541 passed / 0 failed | 3435 | 3465 | (见提交) |
| 2026-09-21 | 第 93 轮：修**"详情页有封面、海报墙显示获取失败"**——根因：详情页 `ComicCover` 不传 `remoteAssetId` ⇒ 走 legacy 纯本地缓存（`read_legacy_cover_local`，键=authority+逻辑路径），墙上卡片走 unified 缓存（state + `cache/cover/<sha256>.cover-v2`），**两套缓存不互通**；修法＝unified 两个抛出点之前各补一次 legacy 纯本地回退（零网络、try/catch、仅 legacy 源）；新增 `test/comic_cover_legacy_fallback_test.dart` 2 条并把修复 stash 掉验证其确实会挂；封面相关 12 文件 49 项全过；桌面重建重启（PID 48300）、APK 重建（手机断开未装）。遗留：unified 侧对这几本 MOBI 仍失败，需桌面刷新后读 `scan_diag.log` 的错误码 | 3476 | 3506 | (见提交) |
| 2026-09-21 | 第 94 轮：**取消"故意整本读"的字节预算拒绝**（`is_deliberate_whole_file`：offset 0 + 一次要完整个文件 + ≤512 MiB；次数 384/时间 30s 两条照旧兜住病态扫描，既有`adapter_byte_source_stops_at_the_read_budget` 未改仍过）+ **拓展整本缓存清理**（`enforce_raw_cache_limit` + 2 GiB 上限 + 最旧优先整包删除 + 始终保留最新包；收口点 `register_book`；封面整本回退是内存读、不落 raw）+ 附带 MOBI **惰性拒绝原因埋点**（`mobi_lazy_declined reason=…`）与**两个过严拒绝条件改跳过**（越界偏移/空区间记录，新增回归用例）。取证：`mobi_diag` 显示 `mode=full-fallback size=82609433`、`scan_diag` 同一时刻 `cover_read_budget_exceeded` | 3525 | 3559 | (见提交) |
