# RCH 问题记录（2026-08-08）

> 本文档记录 2026-08-08 会话中排查/修复过的所有问题，便于后续追溯。
> 代码已按用户要求还原到 v0.4.4（commit 4c7cbe3）。
> 所有未发布的修复保存在备份分支 `codex/wip-2026-08-08`，如需恢复可从此分支找回。

## 1. 115 网页接口错误 50015（漫画无法阅读，含已缓存）

- 现象：打开 115 漫画报「115 网页接口错误(50015)」，缓存过的文件也打不开。
- 根因（数据层）：本地记录的部分 pickcode（如 `akcm*`、`bim2*`）在账号中已解析不到，
  同名文件已被重新上传/替换，旧提取码失效。115 的 50015 即「文件不存在或已删除」
  （p115client 源码确认，同族错误码 20013/20018/31003/70005/70008/90008/430004）。
- 根因（代码层）：打开书籍时第一步联网调 `resolve_name()` 取直链，失败直接抛错，
  本地缓存没有机会被使用，导致缓存过的书也打不开。
- 已做修复（已还原）：缓存命中时直接本地打开，不再先联网；50015 等错误码映射为可读文案。

## 2. 115 文件夹列表把文件全部显示成文件夹

- 现象：115 书源里 `.zip` 等漫画文件被识别为目录，无法正常浏览/打开。
- 根因：115 新版接口的混合列表（show_dir=1）不再返回文件的 `fid`/`s` 字段，
  代码用 `fid 是否为空` 判断目录，导致所有条目都判定为目录。
- 已做修复（已还原）：文件/目录分开请求（show_dir=0 取文件；show_dir=1 混合列表
  按 cid 是否命中文件集合判定目录）。

## 3. 同步拉取显示「远程没有可拉取的包」

- 现象：全量推送成功后，另一端同步拉取提示「远程没有可拉取的包」。
- 根因：导出查询 `updated_at > since` 在全量导出（since=0）时漏掉了 `updated_at=0`
  的旧标签（日漫、1号、been 等），而 book_tags 全部导出并引用这些缺失标签；
  新设备导入时 book_tags 外键（tag_id → tags）中断，异常被拉取逻辑吞掉，
  最终显示「没有可拉取的包」。
- 已做修复（已还原）：导出条件改为 `>=`；导入时对引用缺失标签的关联跳过而非中断。

## 4. 二维码对话框在矮屏溢出黄条

- 现象：MuMu（逻辑 853x480）上 115 扫码对话框内容列底部溢出 61px，出现黄黑溢出条。
- 根因：AlertDialog 内容固定高度（220px 二维码 + 文案），矮屏放不下。
- 已做修复（已还原）：两个扫码对话框加 `scrollable: true`，并新增回归测试
  `test/qr_dialog_short_screen_test.dart`。

## 5. 115 打开慢 + 接口被风控（HTTP 405）

- 现象：115 书源打开很慢；随后 webapi/aps 接口全部返回 405（阿里云 WAF 阻断页），
  匿名请求也被拦（IP 级封锁）。
- 原因：
  1. 文件夹封面检测会对每个子文件夹重复拉取目录，配合目录/文件双请求，
     打开一次根目录就有 10+ 次请求；
  2. 限速 1.5 秒/请求，串行检测导致打开耗时 15 秒以上；
  3. 排查过程的大量请求共同触发 WAF 风控。
- 已做修复（已还原）：目录列表 60s 缓存、限速降到 0.5 秒、请求失败回退旧缓存
  避免白屏、405 提示改为「请求过于频繁」。
- 注意：IP 风控需等待解除（可能数十分钟到一天），期间 115 无法访问。

## 6. MuMu 上 115 书源凭据为空 + 多余书源

- 现象：MuMu 上原 115 书源（root=3481445593719506553）Cookie/refresh_token 为空，
  无法连接；另有一个后添加的 115 书源（root=0，显示云下载/剧场/游戏/艺术等系统目录）。
- 说明：同步包按设计不携带书源凭据（避免泄露），每台设备需要单独扫码登录；
  这不是 bug，但会导致换设备后书源「打不开」的观感。
- 建议：删除多余书源，在需要使用的设备上重新扫码获取 Cookie。

## 7. 会话早期已修复并随 v0.4.4 发布的问题

- WebDAV 同步 URL 基础路径缺失（坚果云需 `/dav` 前缀）→ v0.4.4 已修复。
- Flutter/Rust 内容哈希不一致（`Content hash on Dart side ... Rust side`）→
  Rust 代码变更后需重新 `cargo build --release` 再运行，属构建流程问题。

## 还原操作

- 目标：v0.4.4（tag `v0.4.4`，commit `4c7cbe3`）。
- 方式：未发布修改已提交到备份分支 `codex/wip-2026-08-08`，`master` 已 reset 到 v0.4.4。
- 备份分支内容：115 列表修复、115 缓存/限速优化、同步导出导入修复、二维码溢出修复、
  相关回归测试与诊断工具（examples/repro_115.rs、repro_pull.rs）。

## 后续：选择性恢复（2026-08-08 追加）

经用户确认，在 v0.4.4 基础上只恢复两项低风险修复：

1. **50015 阅读失败**：`open_cloud115_cookie_book` 改为缓存命中时直接本地打开
   （不再先联网取直链）；`map_115_web_errno` 增加 50015 等错误码的可读文案。
2. **二维码矮屏溢出**：两个扫码对话框加 `scrollable: true`，并新增回归测试
   `test/qr_dialog_short_screen_test.dart`。

未恢复（仍在备份分支）：115 列表文件/目录识别修复、列表缓存与限速优化、
同步导出/导入修复、M1/M2 功能。这些改动影响面更大，需在 115 风控解除后
用真实账号逐项验证再考虑合入。

## 问题归因（哪些是本次代码改动引起的）

### A. 本次代码改动直接引起

| 问题 | 机制 |
| --- | --- |
| 115 打开特别慢 | 本次叠加了三层请求放大：M2 文件夹封面检测对每个子目录额外发一次列表请求（新功能）；列表修复改成「文件/目录双请求」（本次修改）；原有 1.5s/请求限速。打开根目录一次约 10+ 次请求、耗时 15s 以上。 |
| 115 接口 405 风控（间接） | 请求量上升 + 调试过程用 curl/repro 高频探测，共同触发阿里云 WAF 对本机 IP 的全端点封锁（webapi/aps 连匿名请求都 405）。405 是外部惩罚，但请求量是本次开发活动造成的。 |

### B. 原有代码就存在的问题（本次未引入，但改动时撞上）

| 问题 | 为什么说原有 |
| --- | --- |
| 50015 阅读失败（含缓存） | `open_cloud115_cookie_book` 第一步就联网 `resolve_name()`，失败即抛错，缓存没机会用。该逻辑 v0.4.4 就有。 |
| 115 文件全部显示成文件夹 | 列表用 `fid 是否为空` 判断目录，而 115 新版接口混合列表不再返回 fid/s。判断代码 v0.4.4 就有，是 115 外部改版后暴露的。 |
| 同步「远程没有可拉取的包」 | 导出查询 `updated_at > since` 在全量（since=0）时漏掉 `updated_at=0` 的旧标签 → 新设备导入 book_tags 外键中断。该查询 v0.4.4 就有，只是此前没在全新设备上拉过全量包。 |
| 二维码对话框矮屏溢出黄条 | AlertDialog 固定高度内容在 853x480 放不下。对话框代码 v0.4.4 就有，换 MuMu 测试才暴露。 |

### C. 外部 / 数据 / 操作因素（与代码无关）

| 问题 | 说明 |
| --- | --- |
| 50015 的数据层原因 | 本地记录的部分 pickcode 已失效——同名文件被重新上传/替换过，旧提取码在账号里解析不到。 |
| 115 WAF IP 封锁 | 需要等待解除（可能数十分钟到一天），期间无法访问。 |
| MuMu 上 115 书源凭据为空 | 同步包按设计不携带凭据，每台设备需单独扫码登录（不是 bug，但容易误判为打不开）。 |
| MuMu 多了一个 root=0 的 115 书源 | 用户自行添加，非代码问题。 |
| FRB 内容哈希不一致 | Rust 改动后未重新编译导致，属构建流程问题。 |

## 未来改动规范（怎么写才不会再踩坑）

1. **改之前先测基线**：动手前先在 v0.4.4（或当前发布版）上复现用户现象，确认是「原有 bug」还是「新引入」，避免责任错位和无效改动。
2. **性能改动先量化**：任何涉及网盘请求的功能，改前统计「一次操作发几次请求、耗时多少」，改后对比。新增封面检测/双请求这类放大请求数的代码，必须同时配缓存与懒加载。
3. **外部接口要防御性解析**：对 115 这类会改版的接口，解析层不得依赖单一字段（如 fid）判断类型；用真实响应样本写解析单测（接口一变测试就红），并保留多端点 fallback。
4. **网络请求统一节流与容错**：列表加短 TTL 缓存 + 失败回退旧缓存；405/风控快速失败并给出明确文案，不要多端点×多页重试放大请求；限速参考 p115client（4 QPS）留余量。
5. **封面检测不阻塞首屏**：先渲染占位，滚动到可见/点击时再加载子目录封面，避免打开文件夹就扫全部子目录。
6. **数据完整性防御**：导出不得因旧数据（updated_at=0）丢实体；导入对单条脏数据（缺标签外键）跳过而非中断整个导入；补「全量包导入到全新数据库」的回归测试。
7. **矮屏布局统一防护**：对话框一律 `scrollable: true` 或内容包滚动视图；新增 853x480 矮屏渲染回归测试。
8. **小步提交、逐批验收**：不要把大量未发布改动攒在一起；每批修复先在目标设备（MuMu）验收并写测试，稳定后再发布。
9. **凭据与同步边界明确**：若希望跨设备共享书源凭据（用户提过共用 refresh_token），应设计显式的「加密凭据导出/导入」机制，而不是让同步包悄悄携带凭据。
