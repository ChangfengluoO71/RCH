## 新增

- **AI 超分**：右键单页 2x 超分 + 整本超分后台队列（多本排队、重启续跑、全局进度悬浮窗、完成提示、随时取消）；阅读器内可随时切换原版与超分版本，页码不丢
- **缓存目录切换重构**：文件夹选择后自动迁移整个根目录（数据库+缓存），数据保留在用户所选目录，重启自动恢复，中断可续迁

## 优化

- AI 引擎加固：60 秒超时、无黑窗、原子缓存写入、JPEG 质量提升至 90
- 标签持久化增强：阅读记录轻量落盘、退出自动保存，重启不再丢标签

## 修复

- 修复重启后标签丢失、保存失败、超分进度跳变、取消超分误清全部缓存、恢复默认目录被拒等问题
