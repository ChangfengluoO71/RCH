# P1 最终报告：A–F 矩阵、门禁证据、Release Gate（RG-A / RG-B）

- **日期**：2026-09-18
- **提交**：`fcb83bc`（P1 主提交，69 files / +13009 / −307）+ `e7cc4c9`（LOG-INDEX hash 回填）
- **分支**：`p1-cover-completion`（已推送；**未 merge**）
- **结论**：**P1_PASS**；**Release Gate PENDING**（按 RG-A / RG-B 拆分，见第 4 节）

## 1. A–F 矩阵

| 子项 | 内容 | 状态 |
|---|---|---|
| P1-A | cover job 状态转移矩阵（`resolve_upsert_state` 单一规则表） | PASS |
| P1-B | source-level wake（ready 但字节缺失 → 对账 → 确有消费者） | PASS |
| P1-C | session-ready 生命周期 + 6h 一次性长期补偿 | PASS |
| P1-D | 统一 disk-first 封面 | PASS |
| P1-D2 | legacy/local offline disk-first + cache authority | PASS（因 unified 生产路径改动，已 fresh 重验） |
| P1-E | 事件驱动卡片状态（post-commit wake / 窄 stream / 只读 state API / 删除轮询） | PASS（7/7） |
| P1-F | 进度聚合语义（available / waiting / other + 不变量 + 精确分母） | PASS |

## 2. fresh 门禁证据

| Gate | 结果 |
|---|---|
| `cargo test --locked -j 2 -- --test-threads=1` | **EXIT=0 · 24 targets · 453 passed / 0 failed / 0 ignored** |
| 契约测试 | **60 条全绿**（Rust 38 + Dart 22） |
| Flutter focused（D2 / E / F / bridge，6 文件） | 22 passed / 0 failed |
| Changed-file `flutter analyze` | No issues found |
| Full `flutter analyze` | 121 issues（与既有基线一致，**无新增**） |
| `cargo clippy --locked --all-targets` | 仅既有 `src/reader.rs:277`；改动文件零发现 |
| rustfmt | 本会话引入偏差的 5 个文件已格式化；`cover_service.rs` 保持原样（HEAD 已 dirty） |
| `delivery_check` | **PASS**（file-exists / file-nonempty / encoding-utf8 / delivery-evidence） |

## 3. 证据边界（不可混为一谈）

1. **Rust notify-decision**：`wake_decided_count()` 证明"production 代码在 **commit 之后**走到了
   notify 决策" —— **不是** delivery 计数，也不是 Dart 收到计数。
2. **Dart 侧消费逻辑**：coordinator 真实 wake 路径覆盖（durable-token 去重 / missed-event
   catch-up / 聚合刷新 / dispose）。
3. **真实 FRB `StreamSink` 跨桥投递**：生产接线存在（`main.dart` 启动订阅，codegen 已生成），
   但**自动化测试未构造真实 sink** ⇒ 归入 **RG-B**（见第 4 节 B-6）。
4. **D2 的 `session`/`provider` 边界**：记录于注入的 legacy 传输边界，而非真实 FRB session 层
   ⇒ 真实边界归入 **RG-B B-5**。
5. **403/405/429**：**处理语义可自动化（RG-A）**；**"真实 115/Quark 是否新增 403/405/429"
   只能由 RG-B 证明** —— 两条证据不可合并。
6. **Family 2 known unrecoverable**：all deterministically recoverable historical cover
   identities are supported. Legacy raw-key covers for 115-web and Quark become unrecoverable
   if their raw file is removed, because the provider-returned filename was never durably
   persisted.（唯一可恢复路径见 `docs/project/TODO.md` 的 raw-cache identity 持久化项）

## 4. Release Gate：RG-A（可自动化/可重复）与 RG-B（真实环境）

> 拆分原则：**先把可重复、可回归的条件固化**，真机/真账号验证才不会混入基础语义错误。

### RG-A — 可在 CI / 本地自动验证

| 编号 | 项 | 判据（可执行） |
|---|---|---|
| A-1 | 20 / 100 / 300 MiB 的 Range 行为与取消清理 | 分块 Range 读取正确；取消后 `.part`、临时文件、内存占用回落（上界断言） |
| A-2 | Range 不可用时的 fallback / 拒绝策略 | 与 ADR-005 一致：回退整包下载到本地缓存，或明确拒绝；无静默错误 |
| A-3 | 403 / 405 / 429 的**处理语义与退避契约** | 状态码 → 错误分类 → 退避/补偿路径的单元与集成断言（**不涉及真实 provider**） |
| A-4 | request-once / wake-read-only / no-polling 的 E2E seam | 现有 harness 已覆盖（REQUEST-ONCE / NO-POLL / F-SCAN-TERMINAL）；补一条 host/integration 级复用 |
| A-5 | cache / `.part` / cancellation / memory 上界 | 缓存与临时文件的创建-清理对称性；内存峰值有上界断言 |

### RG-B — 必须真实 provider / 真账号 / 真网络

| 编号 | 项 |
|---|---|
| B-1 | 115 / Quark 真实登录与读取 |
| B-2 | 实际是否出现**新增** 403 / 405 / 429 |
| B-3 | WAF 行为 |
| B-4 | CDN burst / rate / in-flight 实测 |
| B-5 | D2 的真实 session / provider 边界 |
| B-6 | 真实应用环境中的 FRB stream delivery（若 A 层无法做到真正跨桥验证） |

> **禁止**把 "403/405/429 处理正确（RG-A A-3）" 与 "真实 115/Quark 未出现新的 403/405/429
> （RG-B B-2）" 合成一条证据。

## 5. Release Candidate identity —— **B-0.1 已 materialize**（2026-09-18）

**RC identity：`0.5.8+100508`**（versionName `0.5.8`，base build `100508`）

> 编号约定：RG-B 采用 **B-0…B-4**（本节）。§4 的 B-1…B-6 为旧编号，映射关系：
> 新 B-1 = 旧 B-6（FRB delivery）；新 B-2 = 真实 Range/cancel（§4 未单列）；
> 新 B-3 = 旧 B-1 + 旧 B-5；新 B-4 = 旧 B-2 + 旧 B-3 + 旧 B-4。

### 5.1 两层身份定义（关键）

| 层 | 含义 | 变化的影响 |
|---|---|---|
| **RC Product Commit** | 生产源码 / 运行时 / **构建元数据**的冻结点 | 任何 production / runtime / **build-metadata** 变化 ⇒ 使对应 RG-B runtime evidence **失效**（或至少需重验相关条目） |
| **Validation Harness Commit** | 允许在 RC Product Commit **之后**新增 `integration_test/**`、测试夹具、报告与 LOG | **仅**测试/文档变化**不**使 RC product identity 失效 |

> **失效规则（收紧后）**：任何 production/runtime/build-metadata 变化使对应 RG-B runtime evidence 失效；
> 仅测试/文档变化不使 RC product identity 失效，但**每份证据必须同时记录 product commit 与 harness commit**。

### 5.2 已冻结的提交

| 提交 | 角色 | 内容 |
|---|---|---|
| `81a824e228e4049ec946411429b0855d1e96f8c4` | **pre-version-bump product baseline** | P1 全量 + RG-A 之后的代码状态；元数据仍为 0.5.7 ⇒ **不得**充当 RC 证据的版本身份 |
| **`b8b64de2f905cdd89e93c60a740cfc806df8cfee`** | **RC Product Commit** | `chore(release): materialize RC 0.5.8+100508` —— 唯一改动是 `app/pubspec.yaml` 的版本元数据 |

### 5.3 本地/开发构建身份（B-1 的 Windows integration run 依赖此路径）

`flutter test integration_test/... -d windows` 这类**本地**构建的版本元数据来自 `app/pubspec.yaml`：

| 项 | materialize 前 | materialize 后（当前） |
|---|---|---|
| pubspec | `0.5.7+100507` | **`0.5.8+100508`** |
| Windows FileVersion/ProductVersion | `0.5.7.100507` | **`0.5.8.100508`** |
| Android versionName / versionCode（本地） | `0.5.7` / `100507` | **`0.5.8` / `100508`** |

> 若不在采集 B-1 证据**之前** materialize，该运行证据会带着 0.5.7 元数据，与 candidate 声明自相矛盾。

### 5.4 发布产物身份（来自 tag，而非 pubspec）

`release.yml` 对 **Windows 与 Android 都**传 `--build-name="$ver" --build-number="$code"`：
`ver` = tag 去掉 `v`；Android 的 `code` 由工作流算术得出

```bash
semver_code=$((maj * 10000 + min * 100 + pat))
code=$((100000 + semver_code))     # 必须 > 2507（历史真机基线 2507）
flutter build apk --release --split-per-abi --build-name="$ver" --build-number="$code"
```

对 **v0.5.8**：`semver_code = 508` ⇒ **base versionCode = 100508**（与本地身份一致 ✓）。

| 项 | RC 值 |
|---|---|
| Android `versionName` | `0.5.8`（tag） |
| Android **base** `versionCode` | **100508** |
| Android 各 ABI 最终 `versionCode` | ⚠️ 由 `--split-per-abi` 的 ABI 偏移产生，**必须以构建产物为准**；仓库历史（`LOG.md:623`）确认偏移存在（示例 `arm64=2400`），但**未记录 v0.5.7 的逐 ABI 映射** ⇒ 本报告**不凭推断填写** |
| Windows `FileVersion` / `ProductVersion` | `0.5.8.100508` |
| 下限约束 | `code > 2507` ✓（100508） |

### 5.5 其它冻结事实（2026-09-18 实查）

| 项 | 值 |
|---|---|
| tags | 本地 = 远端，最高 **`v0.5.7`**；**不存在 `v0.5.8`** ✓ |
| GitHub Releases | 最新已发布 = **`v0.5.7`**（`draft=false`、`prerelease=false`），另有 v0.5.6/5/4 ⇒ 候选号未被占用 ✓ |
| Windows 版本宏 | `windows/runner/CMakeLists.txt` 用 `target_compile_definitions` 定义 `FLUTTER_VERSION_*` ⇒ `Runner.rc` 由构建参数派生。⚠️ `Runner.rc` 的 `#else` 回退是**硬编码 `0,4,1,401`**（仅缺宏的非 Flutter 构建可达，属潜在陷阱） |
| build 号约定 | v0.5.3–v0.5.6 为 `+503…+506`，**v0.5.7 起 `+100507`** ⇒ 必须单调递增，**不得**回归 `+508` 形态 |

### 5.6 RG-B 进度

| 编号 | 内容 | 状态 |
|---|---|---|
| **B-0** | RC identity 冻结 | **PASS** |
| **B-0.1** | RC materialization（`0.5.8+100508` 成为可构建身份） | **PASS（`b8b64de`）** |
| **B-1** | **真实应用环境 FRB `StreamSink` delivery** | **PASS**（product `b8b64de` / harness `12c8fb6`） |
| **B-2** | 真实网络 20/100/300 MiB Range + cancel | PENDING |
| **B-3** | 真实 115 / Quark（session / provider / D2 边界） | PENDING |
| **B-4** | WAF / CDN / 403·405·429 / burst-rate-in-flight | PENDING |

### 5.7 B-1 证据（真实 FRB 跨桥 delivery）

**结论**：真实 Rust post-commit wake **确实跨过真实 FRB `StreamSink`** 并被真实 Dart coordinator
消费，UI 在**测试不驱动任何读取**的条件下自动从非 ready 变为 ready。

- **product commit**：`b8b64de2f905cdd89e93c60a740cfc806df8cfee`
- **harness commit**：`12c8fb6d4c9afa63960ceb3e7d00440cc26fbdcc`
- **运行**：`flutter test integration_test/cover_stream_real_delivery_test.dart -d windows`
  → **EXIT=0 / All tests passed（1:00）**
- **机读证据行**：
  `RG_B1_FRB_DELIVERY=PASS;candidate=0.5.8+100508;product_commit=b8b64de;requests=11;injected_stream=false`
- **时序证据（夹具请求日志）**：

```text
卡片挂载前（扫描 terminal）: PROPFIND / · GET / (range probe) · HEAD×3 · PROPFIND / · PROPFIND /Series
                            ⇒ 无任何封面抓取 ⇒ 卡片**确实从非 ready 起步**（RawImage 不存在）
卡片挂载后（wake 驱动）:     GET /Series/001.png bytes=0-0 · bytes=0-66   ← 首次 request（卡片）
                            GET /Series/001.png bytes=0-0 · bytes=0-66   ← **wake 触发的只读重取**
```

- **同时观测**：`flipped=true`（UI 自行渲染）、`durable_state=ready`、`ready_books=1`、
  `available_books=1`（F 语义计数在真实路径上自洽）。
- **辅助证据**：`debugCoverRevisionStream == null`（未注入 fake stream）——仅辅助，
  **不单独构成** transport 证明。
- **消费语义**（dedup / catch-up / request-once / no-polling）仍引用 A-4 的 fresh 13/13，
  B-1 **未**重建该矩阵。
- **两层身份复验**：`git diff b8b64de..12c8fb6` 仅含 `app/integration_test/**` 与 `docs/**`，
  **生产 runtime surface 改动 = 0** ⇒ harness 层未改变 RC product identity ✓

**过程中发现并修正的 harness 缺口（如实记录，均非生产缺陷）**：

| # | 现象 | 层级 | 修正 |
|---|---|---|---|
| 1 | `webdav_connect` 报 `AnyhowException(NotFound)` | 夹具 | 生产 connect 阶段会对 browsing root 做 range probe（`GET`）；夹具原先对 collection 返回 404。改为 collection GET ⇒ **200**（与真实 WebDAV 一致） |
| 2 | `remoteDirectoryView` 报 `source identity unavailable: Query returned no rows` | 夹具/装配 | ADR-020 要求源在 Rust 侧有身份行（`book_sources.fingerprint`）。harness 补上生产注册入口 **`dbUpsertSource`** |
| 3 | `durable_state=ready` 但 UI 不翻转 | **harness 启动** | `startCoverRevisionWatch()`（唯一真实订阅）只在 **`main()`** 中建立；harness 原先仅 `pumpWidget(RchApp)`、**从未执行 `main()`** ⇒ 无订阅 ⇒ wake 无人消费。改为执行**生产 `main()`** 后一次通过 |

> 第 3 条尤其重要：它说明"durable 已 ready 而 UI 冻结"**既可能是** transport 缺口，**也可能是**
> 启动路径未建立订阅。B-1 的 authority 是"**无其他 wake source 时 UI 自动跟随**"，因此任何 harness
> 都必须先执行生产启动（`main()`），否则该断言不成立。

## 6. 后续里程碑顺序

`P1 文档闭环 → 工作树清洁 → RC identity 冻结 → RG-A → RG-B → Release decision`

（本文件即第 1 步产物；第 2 步见本节提交后的 dirty 清理；第 3 步见第 5 节。）

## 7. 基线例外（未修，与 P1 无关）

- Full `flutter test`：6 个文件既有失败（`cover_editor_consent` 缺 `CustomCover*`、
  `add_source_dialog`、`qr_dialog_save`、`folder_snapshot_store`(setUpAll)、
  `record_repository_alias` 缺 `remoteLivePaths`、`update_handoff` 缺 `UpdatePlatform*`）——
  取证：这些符号在 `lib/` 中不存在。
- Full `flutter analyze`：121 issues；`cargo clippy`：`src/reader.rs:277`；
  `cover_service.rs` 的 rustfmt 偏差（HEAD 已 dirty）。

## 8. 相关报告

- `2026-09-18-p1ef-implementation.md`（E/F 实现轮次：语义冻结、落地内容、7 个修复的缺陷）
- `2026-09-17-p1d2-final.md`（D2：cache authority 与 raw-key 结论）
- `2026-09-17-p1ef-phase1-audit.md`、`2026-09-17-p1e-emission-coverage-proof.md`、
  `2026-09-17-p1e-view-revision-reuse-decision.md`（审计期：emission coverage 与 revision 复用判定）
