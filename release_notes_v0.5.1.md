# RCH v0.5.1

## 修复

- **条漫模式页码显示**：滚动阅读时底部新增页码/进度栏（`‹ 页码/总数 ›`），页码随滚动实时更新；底部前后翻页按钮在条漫模式下可真正滚动翻页，点页码可输入跳转到指定页。
- **书源界面顶栏与状态栏重叠**：Android 上点进书源浏览界面，最上面一栏完整显示在状态栏下方，不再与手机时间显示层重叠（平板/大视口布局下尤其明显）。
- **PC 端应用图标**：Windows 应用图标更换为与手机一致的紫底白字「RCH」，任务栏 / 窗口 / 安装包图标统一。

## 其他

- 新增 `build_artifacts/make_app_icon.py`：PC 图标生成工具脚本（紫底白字 RCH，可复现），供后续调整图标时使用。

## 提示

- Android / Windows 均建议在应用内「关于与更新」检查并安装新版本。
