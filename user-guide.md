# RCH 用户手册

RCH 是一个基于 Flutter + Rust 的跨平台漫画阅读与漫画库管理器，支持 Windows 与 Android。

RCH 的核心思路是 **Local-first**：漫画文件仍然保存在用户自己的本地目录、NAS 或云盘中，RCH 负责提供统一的书源、索引、阅读、标签、元数据、缓存和同步能力。

本手册面向普通用户，主要介绍如何安装 RCH、添加漫画、配置远程书源、阅读漫画、管理缓存、使用多设备同步，以及遇到常见问题时如何排查。

---

## 目录

- [1. 开始使用](#1-开始使用)
- [2. 添加本地漫画](#2-添加本地漫画)
- [3. 阅读漫画](#3-阅读漫画)
- [4. 漫画库与标签管理](#4-漫画库与标签管理)
- [5. 添加远程书源](#5-添加远程书源)
  - [5.1 WebDAV](#51-webdav)
  - [5.2 SMB](#52-smb)
  - [5.3 SFTP](#53-sftp)
  - [5.4 百度网盘](#54-百度网盘)
  - [5.5 115 网盘](#55-115-网盘)
  - [5.6 夸克网盘](#56-夸克网盘)
- [6. 远程阅读策略](#6-远程阅读策略)
- [7. 缓存管理](#7-缓存管理)
- [8. AI 2× 超分辨率](#8-ai-2-超分辨率)
- [9. 多设备同步](#9-多设备同步)
- [10. 离线索引](#10-离线索引)
- [11. 数据备份与迁移](#11-数据备份与迁移)
- [12. 更新与版本升级](#12-更新与版本升级)
- [13. 常见问题](#13-常见问题)
- [14. 隐私与安全](#14-隐私与安全)

---

# 1. 开始使用

## 1.1 安装

### Windows

从 GitHub Releases 下载最新 Windows 安装包并运行。

安装完成后启动 RCH。

首次启动后建议进入：

`设置 → 关于与更新`

检查当前版本。

如果 GitHub 下载速度较慢，可以在：

`设置 → 关于与更新 → 下载通道`

选择可用的镜像通道。

### Android

下载最新 APK 后安装。

Android 首次安装可能需要允许“安装未知应用”。

现代 Android 手机通常选择 `arm64-v8a` 架构；如果设备较旧，则根据实际 CPU 架构选择对应 APK。

---

## 1.2 第一次启动

第一次使用 RCH 时，建议按以下顺序配置：

1. 添加一个本地漫画目录，确认阅读链路正常。
2. 根据需求添加 WebDAV、NAS 或网盘书源。
3. 设置远程阅读策略。
4. 根据设备存储情况调整缓存目录。
5. 有多台设备时，再配置同步。

这样可以先确认本地阅读正常，再逐步增加远程书源和同步功能。

---

# 2. 添加本地漫画

RCH 支持直接读取本地漫画文件，也支持将包含漫画图片的文件夹识别为漫画。

进入：

`书源 → + → 本地目录`

选择漫画所在目录，然后添加。

RCH 支持的常见格式包括：

- ZIP / CBZ
- EPUB
- CB7 / 7Z
- CBT / TAR
- PDF
- CBR / RAR
- MOBI / AZW / AZW3
- 图片文件夹

---

## 2.1 压缩包漫画

对于 ZIP / CBZ / EPUB 等格式，RCH 不需要在启动阅读器之前把整个压缩包完整解压。

程序会先读取压缩包目录，然后按页面需要读取内容。

因此，对于几百 MB 的漫画压缩包，通常可以直接打开阅读。

---

## 2.2 文件夹漫画

如果一个目录中包含漫画图片，RCH 可以将该目录识别为漫画。

例如：

```text
My Comic/
├── 001.jpg
├── 002.jpg
├── 003.jpg
└── 004.jpg
```

如果目录中存在：

```text
ComicInfo.xml
```

或：

```text
metadata.json
```

RCH 会尝试读取其中的标题、作者、系列、类别等元数据。

---

## 2.3 自动转换 CBZ

RCH 支持在本地刷新过程中将部分漫画文件夹或 ZIP 转换成 CBZ。

这一功能可以在全局设置中关闭。

如果你希望保留原始目录结构，则建议关闭自动转换。

---

# 3. 阅读漫画

点击书源中的漫画即可进入详情页，然后开始阅读。

---

## 3.1 阅读模式

RCH 提供三种主要阅读方向：

### 日漫

从右向左翻页。

### 美漫

从左向右翻页。

### 条漫

纵向连续滚动。

可以根据漫画类型切换阅读模式。

---

## 3.2 双页模式

在 Windows 或平板等较大屏幕设备上，可以启用双页模式。

双页模式会尽可能将相邻页面并排显示。

对于日漫等需要独立处理第一页的漫画，可以单独设置首页显示策略。

---

## 3.3 页面操作

阅读过程中可以：

- 放大
- 缩小
- 拖动
- 旋转页面
- 单独保存页面旋转状态

Windows 下可以使用鼠标、触控板和键盘完成主要阅读操作。

Android 下可以直接通过触控完成缩放和翻页。

---

## 3.4 阅读进度

RCH 会保存阅读状态，包括：

- 当前页
- 阅读位置
- 阅读时间
- 阅读次数
- 已读状态

重新打开漫画后可以继续上次的阅读位置。

---

# 4. 漫画库与标签管理

RCH 不只是一个文件浏览器。

每本漫画可以拥有自己的元数据和标签。

---

## 4.1 标签

标签按照类型进行分组，例如：

- 作者
- 类别
- 系列
- 状态
- AI 超分
- 自定义标签

标签管理页可以查看每个标签关联的漫画数量和阅读次数。

---

## 4.2 已读状态

打开漫画后，RCH 可以自动记录为已读。

也可以对多本漫画进行批量标记。

对于文件夹漫画，批量操作可以递归处理。

---

## 4.3 搜索与筛选

书源和漫画库支持：

- 关键词搜索
- 标签筛选
- 组合条件筛选
- 按字母排序
- 按加入时间排序

其中数字排序采用数字感知方式，因此包含 `2` 和 `10` 的书名不会简单按照字符串顺序排列。

---

## 4.4 封面

本地漫画可以生成海报墙。

远程书源采用懒加载方式获取封面。

已经下载或者缓存过的内容可以优先使用本地缓存，从而减少重复网络请求。

---

# 5. 添加远程书源

进入：

`书源 → +`

在“添加书源”对话框中选择对应类型。

目前支持：

- WebDAV
- SMB
- SFTP
- 百度网盘
- 115 网盘
- 夸克网盘

添加时 RCH 会进行连通性测试。

测试成功后，书源会出现在左侧书源列表中。

---

# 5.1 WebDAV

WebDAV 适合：

- NAS
- 自建服务器
- Nextcloud
- 支持 WebDAV 的云盘
- OpenList / Alist 等中间层服务

进入：

`书源 → + → WebDAV`

填写：

```text
服务器地址
用户名
密码
初始路径（可选）
```

例如：

```text
https://nas.example.com/dav/
```

或者局域网 NAS：

```text
https://192.168.1.10:5006/dav/
```

### Range 支持

RCH 会自动检测服务器是否支持 HTTP Range 分段读取。

如果支持：

```text
远程服务器
    ↓
Range 请求
    ↓
按页面读取
```

可以直接进行流式阅读。

如果不支持，则会回退到：

```text
远程服务器
    ↓
整本下载
    ↓
本地缓存
    ↓
阅读
```

---

# 5.2 SMB

SMB 适用于 Windows 局域网共享以及 NAS。

进入：

`书源 → + → SMB`

填写共享目录路径。

例如：

```text
\\192.168.1.10\comic
```

路径必须使用 UNC 格式，并以：

```text
\\
```

开头。

连接测试成功后即可浏览共享目录。

---

# 5.3 SFTP

SFTP 适用于具有 SSH 服务的 Linux、NAS 或服务器。

进入：

`书源 → + → SFTP`

填写：

```text
服务器地址
端口
用户名
密码
初始路径
```

例如：

```text
服务器：192.168.1.10
端口：22
用户名：media
密码：********
初始路径：/data/comics
```

如果地址本身包含端口，也可以直接使用类似：

```text
nas.example.com:2222
```

---

# 5.4 百度网盘

RCH 通过百度开放平台接口访问百度网盘。

首次使用需要准备自己的开发者应用凭据。

## 第一步：创建百度开放平台应用

打开百度网盘开放平台：

https://pan.baidu.com/union/

登录后进入开发者控制台。

根据平台要求创建应用，并申请网盘基础服务权限。

获取：

```text
AppKey
SecretKey
```

RCH 不提供内置 AppKey / SecretKey。

---

## 第二步：在 RCH 中添加

进入：

`书源 → + → 百度网盘`

填写需要挂载的根目录。

然后展开高级选项，填写：

```text
AppKey
SecretKey
```

点击：

`授权登录`

浏览器会打开百度授权页面。

完成授权后，将授权页面显示的授权码复制回 RCH，再点击：

`换取 token`

成功后，`refresh_token` 会自动保存。

最后点击：

`添加`

即可完成配置。

---

## 第三步：选择漫画目录

百度开放平台对第三方应用目录存在访问限制。

通常建议将漫画放在：

```text
/apps/<产品名>/
```

例如创建应用时产品名为：

```text
RCH
```

则可以将漫画放在：

```text
/apps/RCH/
```

然后将 RCH 的根目录设置为：

```text
/apps/RCH
```

也可以尝试挂载根目录，但实际可访问内容取决于百度开放平台当前权限规则。

---

## token 失效

如果百度登录状态失效：

1. 打开对应书源。
2. 点击书源页面顶部的钥匙按钮。
3. 尝试刷新 token。

如果 refresh token 已经失效，RCH 会重新打开授权流程。

重新授权即可恢复。

---

# 5.5 115 网盘

RCH 支持两种 115 连接方式：

### 方式 A：扫码获取 Cookie

这是默认方式，不需要申请 APP ID。

### 方式 B：官方 APP ID

通过 115 开放平台的官方授权机制登录。

两种方式各有优缺点。

| 项目 | 扫码 Cookie | APP ID |
|---|---|---|
| 是否需要申请开发者 | 否 | 是 |
| 登录方式 | 扫码 | 官方授权 |
| 凭证 | Cookie | refresh_token |
| 稳定性 | 取决于 Web 接口变化 | 官方 API |
| 失效处理 | 重新扫码 | 重新授权 |

---

## 方式 A：扫码获取 Cookie

进入：

`书源 → + → 115 网盘`

将根文件夹 ID 留空，即表示挂载网盘根目录。

也可以填写具体文件夹 ID，只挂载某一个目录。

然后点击：

`扫码获取 Cookie`

RCH 会显示二维码。

使用 115 App 扫码并确认登录。

如果 115 App 对截图二维码识别存在问题，可以尝试使用微信扫码或通过 115 小程序确认。

完成后 Cookie 会自动填写。

点击：

`添加`

即可。

---

## 方式 B：APP ID

首先到 115 开放平台申请 APP ID。

然后在 RCH 中展开高级选项。

填写：

```text
APP ID
```

点击：

`官方模式：APP ID 扫码授权`

完成授权后，refresh token 会自动填写。

之后点击：

`添加`

---

## 115 凭证注意事项

Cookie 和 refresh token 都属于登录凭证。

当前 RCH 会将这些凭证保存在本机 SQLite 数据中，因此：

**不要把 RCH 数据库文件公开上传，也不要把包含凭证的备份发送给其他人。**

此外：

- Cookie 可能过期，需要重新扫码。
- APP ID 模式下重复授权可能使旧 refresh token 失效。
- 某些登录方式会受到 115 服务端规则变化影响。

---

# 5.6 夸克网盘

RCH 当前通过浏览器 Cookie 认证访问夸克网盘。

进入：

`书源 → + → 夸克网盘`

---

## 获取 Cookie

首先在浏览器中打开：

https://pan.quark.cn/

登录夸克网盘。

然后：

1. 按 `F12` 打开开发者工具。
2. 进入 `Network / 网络`。
3. 刷新页面。
4. 点击任意一个夸克网盘请求。
5. 在请求头中找到 `Cookie`。
6. 将完整 Cookie 复制下来。

通常类似：

```text
stoken=...; pds=...; __puus=...
```

将整段 Cookie 粘贴到 RCH 的：

`Cookie`

字段。

根文件夹 ID 默认：

```text
0
```

也可以填写具体目录 ID。

最后点击：

`添加`

---

## Cookie 失效

夸克 Cookie 相当于登录凭证，并非永久有效。

如果出现：

- 书架无法打开
- 登录状态失效
- 网络请求失败

重新从：

```text
pan.quark.cn
```

获取最新 Cookie，再编辑书源即可。

**不要在 Issue、截图、日志或公开仓库中泄露 Cookie。**

---

# 6. 远程阅读策略

RCH 可以统一设置远程书源打开方式。

进入：

`设置 → 远程书源 → 打开策略`

目前有三种模式。

---

## 自动

默认模式。

逻辑：

```text
尝试整本下载
      ↓
成功 → 本地缓存 → 阅读
      ↓
失败
      ↓
尝试流式阅读
```

适合大多数用户。

---

## 优先下载整本

先将漫画完整下载到本地，然后再阅读。

适合：

- 网络较快
- 想一次下载后反复阅读
- 想离线阅读
- 不希望阅读时受到网络波动影响

缺点是第一次打开可能需要等待下载完成。

---

## 直接流式

打开漫画时直接进行网络读取。

适合：

- 网络速度稳定
- 不希望一次性占用大量缓存空间
- 服务端支持 Range

如果服务端不支持随机读取，则流式策略可能无法正常使用。

---

# 7. 缓存管理

RCH 会使用本地缓存降低重复网络请求和重复解析成本。

缓存主要分为：

- 页面缓存
- 整本下载
- 封面缩略图
- AI 超分结果
- 临时文件

进入：

`设置 → 缓存管理`

可以查看不同类型占用的空间。

可以按照类型单独清理。

---

## 清理缓存会不会删除漫画？

不会。

缓存只是 RCH 为了提高访问速度创建的本地副本。

清理缓存：

```text
不会删除远程漫画
不会删除本地原文件
不会删除书源
不会删除阅读记录
```

但是重新访问远程漫画时，可能需要再次下载。

---

# 8. AI 2× 超分辨率

RCH 支持 Windows 上的 AI 2× 超分辨率。

当前使用端侧推理方式处理漫画页面。

进入相关设置后可以启用 AI 超分能力。

---

## 工作方式

```text
原始漫画页面
      ↓
AI Super Resolution
      ↓
2× 放大
      ↓
本地缓存
```

原始漫画不会被覆盖。

超分结果单独保存，因此可以在原图和超分结果之间切换。

---

## 为什么只支持部分平台？

AI 超分会消耗较多 CPU/GPU 资源。

当前版本主要针对 Windows 桌面环境提供完整能力。

Android 端的 AI 能力仍然处于后续探索阶段。

---

# 9. 多设备同步

RCH 的同步主要同步：

- 书源
- 漫画元数据
- 标签
- 阅读记录
- 设置
- 离线索引

**不同步漫画文件本体。**

因此：

```text
Windows
  ├── 本地漫画
  └── 本地数据库
          │
          ▼
        WebDAV
          │
          ▼
Android
  ├── 本地漫画
  └── 本地数据库
```

两台设备仍然可以保留各自的漫画文件。

---

## 9.1 配置同步

进入：

`设置 → 同步`

填写：

```text
WebDAV 地址
账号
密码
远程目录
设备名称
```

例如：

```text
WebDAV 地址：
https://dav.jianguoyun.com/dav/

远程目录：
RCH同步

设备名称：
Windows 主机
```

多台设备必须使用：

**同一个 WebDAV 地址、同一个账号、同一个同步目录。**

设备名称则应该设置成不同名称，例如：

```text
Windows
Android
Tablet
```

---

## 9.2 第一次同步

建议先选择数据最完整的一台设备。

在该设备：

`设置 → 同步 → 立即同步`

首次同步会创建同步库。

远端出现：

```text
manifest.json
state/
```

说明同步库已经建立。

**不要手动修改这些同步文件。**

---

## 9.3 第二台设备

在第二台设备安装相同版本的 RCH。

配置相同的：

```text
WebDAV
账号
密码
同步目录
```

然后点击：

`立即同步`

RCH 会将本地状态与远端同步库进行合并。

---

## 9.4 后续同步

正常使用时无需每次手动点击同步。

RCH 会在后台进行轻量检查，并在本机发生变化时同步。

---

# 10. 离线索引

离线索引不是漫画文件缓存，而是：

**远程书源目录树的本地快照。**

主要记录：

```text
路径
文件名
文件大小
```

它的作用是让你在没有网络时仍然可以看到已经建立索引的云端漫画条目。

---

## 10.1 云端书源

对于 WebDAV、115、夸克等远程书源：

默认情况下：

```text
打开书源
    ↓
在线浏览
```

浏览过的目录，以及阅读、缓存、打标签过的漫画，会逐渐加入本地离线索引。

---

## 10.2 手动生成

在书源浏览页面可以使用：

`⚡ 生成离线索引（本地快照，不联网）`

根据已有本地记录生成离线索引。

---

## 10.3 全量索引

如果需要一次性建立整个远程目录：

`全量重建索引（联网）`

这个操作需要访问远程服务器。

如果漫画数量非常大，执行时间也会明显增加。

---

## 10.4 离线浏览

进入书源后切换到离线浏览模式，即可使用本地索引。

离线模式下：

- 可以浏览已经索引的目录
- 可以查看已经索引的漫画
- 无法访问尚未同步到本地索引的新内容

退出离线模式即可恢复在线浏览。

---

## 10.5 离线索引同步

离线索引本身也可以通过 RCH 同步。

例如：

```text
Windows
  在线浏览某个远程目录
      ↓
生成离线索引
      ↓
同步
      ↓
Android
```

Android 可以直接看到 Windows 已经建立索引的目录，即使 Android 此时没有访问服务器。

---

# 11. 数据备份与迁移

RCH 的本地数据库保存：

- 阅读记录
- 标签
- 元数据
- 书源
- 设置
- 同步状态

部分数据可以进行 JSON / rchpkg 等形式的备份。

涉及版本升级、同步重置等较大操作时，建议先备份。

---

## 11.1 数据目录迁移

进入设置中的数据目录相关选项，可以迁移整个数据根目录。

迁移对象包括：

```text
SQLite 数据库
页面缓存
封面缓存
下载缓存
AI 超分缓存
临时数据
```

适合以下情况：

- C 盘空间不足
- SSD 容量不足
- 希望将缓存放到机械硬盘
- 更换磁盘

建议使用 RCH 自带的迁移功能，而不要直接手动搬动数据库文件。

---

# 12. 更新与版本升级

RCH 可以在：

`设置 → 关于与更新`

检查新版本。

部分版本支持直接在应用内下载并安装更新。

---

## v0.4.x → v0.5.0

这是一个需要特别注意的升级。

v0.5.0 重构了同步协议，因此：

**旧版同步状态不能直接继续使用。**

升级前建议：

1. 导出重要数据备份。
2. 安装新版本。
3. 按同步重置流程清理旧同步状态。
4. 清空旧的远程同步目录。
5. 选择数据最完整的设备重新建立同步库。
6. 其他设备重新加入。

这个过程不会主动删除你的漫画文件。

---

# 13. 常见问题

## Q：打开远程漫画很慢怎么办？

首先检查：

1. 当前远程源是否支持 Range。
2. 当前打开策略是否为“优先下载整本”。
3. 网络是否稳定。
4. 本地缓存是否已经命中。

对于网络较好的环境，可以选择：

`直接流式`

对于想稳定阅读的情况，可以选择：

`优先下载整本`

---

## Q：百度网盘提示需要重新授权

打开对应书源。

点击顶部钥匙按钮尝试刷新 token。

如果 refresh token 已经失效，则重新完成百度授权。

---

## Q：夸克书源突然打不开

重新从：

https://pan.quark.cn/

获取最新 Cookie。

然后编辑书源并替换 Cookie。

---

## Q：115 书源突然失效

如果使用扫码 Cookie 模式：

重新扫码获取 Cookie。

如果使用 APP ID 模式：

重新完成授权。

部分情况下，同账号重复授权可能导致旧 refresh token 被顶掉。

---

## Q：115 扫码识别失败

如果 115 App 对截图二维码识别不稳定，可以尝试：

- 使用微信扫一扫
- 使用 115 小程序
- 直接使用另一台设备扫描

二维码有效时间较短，因此生成二维码后建议立即完成确认。

---

## Q：同步提示 HTTP 409 AncestorsNotFound

首先检查：

```text
同步目录是否存在
同步目录名称是否正确
```

尤其注意目录名中是否混入隐藏控制字符。

重新手动输入同步目录名称，而不是直接从其他地方复制。

---

## Q：同步提示 HTTP 503 Too many requests

通常是 WebDAV 服务端触发请求限流。

RCH 会进行失败退避和重试。

通常无需删除同步数据。

等待一段时间后再次同步即可。

---

## Q：同步目录出现空文件怎么办？

这通常意味着远端仍然残留旧版同步协议生成的状态。

不要手动删除某几个 state 文件。

建议按照“升级与重置同步”流程完整重建同步库。

---

## Q：离线索引为空

远程书源需要先建立本地索引。

可以：

1. 在线浏览目标目录。
2. 打开并阅读漫画。
3. 给漫画添加标签。
4. 使用“生成离线索引”。
5. 必要时执行“全量重建索引”。

本地书源一般会自动扫描。

---

## Q：清理缓存后漫画消失了吗？

没有。

清理缓存只删除本地缓存文件。

远程漫画仍然位于原来的远程服务中，本地漫画也不会被删除。

---

## Q：删除书源会发生什么？

删除书源不仅会移除该书源配置，还会清理该源关联的：

- 阅读记录
- 元数据
- 数据库条目

因此，删除前请确认你确实不再需要该书源的数据。

---

# 14. 隐私与安全

RCH 没有自己的账号系统。

普通使用场景下，阅读记录、标签、元数据和应用设置保存在本地 SQLite。

RCH 不会把用户漫画上传到 RCH 自己的服务器。

但是：

**远程书源凭证需要特别注意。**

部分书源使用：

- Cookie
- refresh token
- 用户名 / 密码

当前实现会将相关登录凭证保存在本地 SQLite 中。

因此不要：

- 将数据库上传到公开仓库
- 将数据库发送给陌生人
- 在 Issue 中上传完整数据库
- 在截图中暴露 Cookie
- 将 Cookie 或 token 粘贴到公开聊天记录

多设备同步不会同步这些书源登录凭证，因此每台设备需要单独完成远程书源授权。

---

# 附录：推荐使用方式

如果你只是想把本地漫画整理好并阅读：

```text
安装 RCH
  ↓
添加本地目录
  ↓
扫描漫画
  ↓
开始阅读
```

如果你的漫画主要放在 NAS：

```text
安装 RCH
  ↓
添加 SMB / WebDAV
  ↓
选择远程阅读策略
  ↓
开始阅读
```

如果漫画主要放在网盘：

```text
安装 RCH
  ↓
添加百度 / 115 / 夸克
  ↓
配置认证
  ↓
选择下载或流式
  ↓
开始阅读
```

如果需要 Windows + Android 双端：

```text
Windows 配置完整书架
        ↓
WebDAV 同步
        ↓
Android 加入同一同步库
        ↓
标签 / 记录 / 元数据 / 索引跨设备同步
```

如果设备存储空间有限：

```text
远程书源
   ↓
直接流式
   ↓
减少整本下载
   ↓
定期清理缓存
```

RCH 的基本原则可以概括为：

> **漫画文件由你自己保存，RCH 负责把这些文件变成一个可以阅读、整理、搜索和跨设备使用的漫画库。**